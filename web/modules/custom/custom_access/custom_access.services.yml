services:
  custom_access.custom_access_services:
    class: Drupal\custom_access\Access\CustomAccessServices
    # arguments: ['@config.factory']
    tags:
      - { name: access_check, applies_to: _hello_world_access_check } 