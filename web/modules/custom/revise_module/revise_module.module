<?php

/**
 * @file
 * Primary module hooks for revise_module module.
 */
function revise_module_theme($existing, $type, $theme, $path){
    return [
        'customer-form' => [
            'variables' => [
                'formData' => NULL,
                'title' => NULL,
            ],
        ],

        // other theme is start from here
    ];
}
